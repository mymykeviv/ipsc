// Centralized feature flags for gradual rollouts
// Toggle flags here or wire to environment-specific configs as needed

export const featureFlags = {
  // Controls showing aggregated INR-only invoice summary totals on the Invoices page
  invoicesSummaryTotals: true,
}
