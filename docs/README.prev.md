IPSC

Local development:

1) Backend tests
   - cd backend
   - python -m venv .venv && source .venv/bin/activate
   - pip install -r requirements.txt
   - pytest

2) Run stack (Docker)
   - docker compose up --build
# ipsc